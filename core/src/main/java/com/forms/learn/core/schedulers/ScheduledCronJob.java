/*
 * package com.forms.learn.core.schedulers;
 * 
 * import java.util.logging.Logger;
 * 
 * import org.osgi.service.component.annotations.Component; import
 * org.osgi.service.component.propertytypes.ServiceDescription; import
 * org.osgi.service.log.LoggerFactory; import org.osgi.annotation.*;
 * 
 * @Component
 * 
 * @ServiceDescription(value = Runnable.class)
 * 
 * @Property( name = "scheduler.expression", value = "0 * * * * ?") public class
 * ScheduledCronJob implements Runnable {
 *//** Default log. *//*
						 * protected final Logger log = LoggerFactory.getLogger(this.getClass());
						 * 
						 * public void run() {
						 * log.info("Executing a cron job (job#1) through the whiteboard pattern"); }
						 * 
						 * }
						 */